{
  "moduleId": "deploy",
  "name": "deploy_version_deployed_instance",
  "label": "已发布的版本实例表",
  "description": "已发布的版本实例表 可用于发布相关的关联查询等报表查询场景。",
  "fields": [
    {
      "name": "id",
      "type": "bigint",
      "description": "id，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "resource_id",
      "type": "bigint",
      "description": "实例id，已建立普通索引，适合用于过滤、关联或排序查询。"
    },
    {
      "name": "version_id",
      "type": "bigint",
      "description": "版本id。"
    },
    {
      "name": "env_id",
      "type": "bigint",
      "description": "环境id。"
    },
    {
      "name": "deploy_user",
      "type": "char(32)",
      "description": "发布人。"
    },
    {
      "name": "deploy_time",
      "type": "timestamp(3)",
      "description": "发布时间。"
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "id"
      ],
      "description": "主键索引，覆盖 id 字段，适合用于唯一定位记录和主表关联查询。"
    },
    {
      "name": "uk_instance_version",
      "type": "BTREE",
      "unique": true,
      "primary": false,
      "columns": [
        "resource_id",
        "version_id"
      ],
      "description": "唯一索引，覆盖 resource_id、version_id 字段，适合用于精确检索、唯一性判断和去重查询。"
    }
  ]
}
