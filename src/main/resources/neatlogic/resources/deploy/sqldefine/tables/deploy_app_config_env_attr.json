{
  "moduleId": "deploy",
  "name": "deploy_app_config_env_attr",
  "label": "发布应用环境属性",
  "description": "发布应用环境属性 可用于发布相关的时间维度统计、关联查询等报表查询场景。",
  "fields": [
    {
      "name": "app_system_id",
      "type": "bigint",
      "description": "应用id，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "app_module_id",
      "type": "bigint",
      "description": "模块id，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "env_id",
      "type": "bigint",
      "description": "环境资产id，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "key",
      "type": "varchar(50)",
      "description": "变量名，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "type",
      "type": "varchar(50)",
      "description": "变量类型。"
    },
    {
      "name": "value",
      "type": "varchar(200)",
      "description": "变量值。"
    },
    {
      "name": "lcd",
      "type": "timestamp(3)",
      "description": "最后一次修改时间。"
    },
    {
      "name": "lcu",
      "type": "char(32)",
      "description": "最后一次修改人。"
    },
    {
      "name": "update_time",
      "type": "bigint",
      "description": "最后一次修改时间。"
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "app_system_id",
        "app_module_id",
        "env_id",
        "key"
      ],
      "description": "主键索引，覆盖 app_system_id、app_module_id、env_id、key 字段，适合用于唯一定位记录和主表关联查询。"
    }
  ]
}
