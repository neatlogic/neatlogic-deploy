{
  "moduleId": "deploy",
  "name": "deploy_app_config_env_attr",
  "label": "Release Application Environment Attributes",
  "description": "Release application environment attributes for reporting scenarios related to time dimension statistics, association queries, etc.",
  "fields": [
    {
      "name": "app_system_id",
      "type": "bigint",
      "description": "Application ID, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "app_module_id",
      "type": "bigint",
      "description": "Module ID, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "env_id",
      "type": "bigint",
      "description": "Environment_asset_id, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "key",
      "type": "varchar(50)",
      "description": "Variable name, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "type",
      "type": "varchar(50)",
      "description": "Variable type."
    },
    {
      "name": "value",
      "type": "varchar(200)",
      "description": "Variable values."
    },
    {
      "name": "lcd",
      "type": "timestamp(3)",
      "description": "Last_modified_time."
    },
    {
      "name": "lcu",
      "type": "char(32)",
      "description": "Last modified by."
    },
    {
      "name": "update_time",
      "type": "bigint",
      "description": "Last_modified_time."
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "app_system_id",
        "app_module_id",
        "env_id",
        "key"
      ],
      "description": "Primary key index covering the app_system_id, app_module_id, env_id, key fields, suitable for uniquely identifying records and queries that join the primary table."
    }
  ]
}
