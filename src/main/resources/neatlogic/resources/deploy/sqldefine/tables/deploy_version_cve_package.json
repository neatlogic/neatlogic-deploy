{
  "moduleId": "deploy",
  "name": "deploy_version_cve_package",
  "label": "发布version_cve_package表",
  "description": "发布version_cve_package表，属于发布模块的数据表。 可用于发布相关的关联查询等报表查询场景。",
  "fields": [
    {
      "name": "cve_id",
      "type": "bigint",
      "description": "CVE的ID，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "package_name",
      "type": "varchar(255)",
      "description": "包名，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "url",
      "type": "varchar(500)",
      "description": "包详情页地址。"
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "cve_id",
        "package_name"
      ],
      "description": "主键索引，覆盖 cve_id、package_name 字段，适合用于唯一定位记录和主表关联查询。"
    }
  ]
}
