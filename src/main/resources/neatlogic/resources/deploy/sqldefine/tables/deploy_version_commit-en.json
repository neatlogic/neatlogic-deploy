{
  "moduleId": "deploy",
  "name": "deploy_version_commit",
  "label": "Release Version Commit Log Association",
  "description": "Version submission records to be associated. This feature can be used for related queries related to releases, such as reporting.",
  "fields": [
    {
      "name": "version_id",
      "type": "bigint",
      "description": "Version_id, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "commit_id",
      "type": "varchar(255)",
      "description": "Submit_id, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "repository_id",
      "type": "bigint",
      "description": "Warehouse_id, used as the primary key or unique identifier for records in this table."
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "version_id",
        "commit_id",
        "repository_id"
      ],
      "description": "Primary key index covering the version_id, commit_id, repository_id fields, suitable for uniquely identifying records and queries that join the primary table."
    }
  ]
}
