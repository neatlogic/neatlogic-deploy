{
  "moduleId": "deploy",
  "name": "deploy_version_dependency",
  "label": "发布版本依赖",
  "description": "发布版本依赖 可用于发布相关的关联查询等报表查询场景。",
  "fields": [
    {
      "name": "id",
      "type": "bigint",
      "description": "主键，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "version_id",
      "type": "bigint",
      "description": "版本id，已建立普通索引，适合用于过滤、关联或排序查询。"
    },
    {
      "name": "package_id",
      "type": "bigint",
      "description": "关联deploy_pkg表中id。"
    },
    {
      "name": "scope",
      "type": "enum('compile','provided','runtime','test','system','import')",
      "description": "依赖作用域，默认compile。"
    },
    {
      "name": "parent_id",
      "type": "bigint",
      "description": "父依赖id，关联deploy_pkg表中id。"
    },
    {
      "name": "build_time",
      "type": "timestamp(3)",
      "description": "编译时间。"
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "id"
      ],
      "description": "主键索引，覆盖 id 字段，适合用于唯一定位记录和主表关联查询。"
    },
    {
      "name": "idx_version_id",
      "type": "BTREE",
      "unique": false,
      "primary": false,
      "columns": [
        "version_id"
      ],
      "description": "普通索引，覆盖 version_id 字段，适合用于过滤、关联或排序查询。"
    },
    {
      "name": "uk_version_id_package_id",
      "type": "BTREE",
      "unique": true,
      "primary": false,
      "columns": [
        "version_id",
        "package_id"
      ],
      "description": "唯一索引，覆盖 version_id、package_id 字段，适合用于精确检索、唯一性判断和去重查询。"
    }
  ]
}
