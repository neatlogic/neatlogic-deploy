{
  "moduleId": "deploy",
  "name": "deploy_version_appbuild_credential",
  "label": "Release Version Application System Build Certification Association Table",
  "description": "The release version application system compilation authentication association table is used for querying, filtering, and reporting on related data for release purposes.",
  "fields": [
    {
      "name": "proxy_to_url",
      "type": "varchar(255)",
      "description": "Jump to URL, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "user_uuid",
      "type": "char(32)",
      "description": "Authenticate the user."
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "proxy_to_url"
      ],
      "description": "Primary key index covering the proxy_to_url field, suitable for uniquely identifying records and queries that join the primary table."
    }
  ]
}
