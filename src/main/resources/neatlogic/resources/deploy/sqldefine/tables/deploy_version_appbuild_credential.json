{
  "moduleId": "deploy",
  "name": "deploy_version_appbuild_credential",
  "label": "发布版本应用系统编译认证关联表",
  "description": "发布版本应用系统编译认证关联表 可用于发布相关数据的查询、筛选和报表展示。",
  "fields": [
    {
      "name": "proxy_to_url",
      "type": "varchar(255)",
      "description": "跳转url，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "user_uuid",
      "type": "char(32)",
      "description": "跳转认证用户。"
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "proxy_to_url"
      ],
      "description": "主键索引，覆盖 proxy_to_url 字段，适合用于唯一定位记录和主表关联查询。"
    }
  ]
}
