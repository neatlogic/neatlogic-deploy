{
  "moduleId": "deploy",
  "name": "deploy_version_cve_package",
  "label": "Release version_cve_package Table",
  "description": "The release version_cve_package table belongs to the deployment module and supports relational queries and other release-related reporting scenarios.",
  "fields": [
    {
      "name": "cve_id",
      "type": "bigint",
      "description": "CVE ID, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "package_name",
      "type": "varchar(255)",
      "description": "Package name, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "url",
      "type": "varchar(500)",
      "description": "Package details page address."
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "cve_id",
        "package_name"
      ],
      "description": "Primary key index covering the cve_id, package_name fields, suitable for uniquely identifying records and queries that join the primary table."
    }
  ]
}
